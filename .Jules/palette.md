## 2026-06-09 - [Skip-to-Content Link]
**Learning:** Keyboard users and screen reader users often need a way to bypass repetitive navigation menus to reach the main content directly. Without this, accessibility is severely hindered.
**Action:** Always add a `Skip to content` link at the top of the body, visually hidden (`sr-only`) but visible on focus, targeting the main content wrapper.
